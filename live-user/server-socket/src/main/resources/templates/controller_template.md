@CrossOrigin(origins = {"${custom.host.frontend.origin}"})



@RestController
@CrossOrigin()
@RequestMapping("/api/")

    @Autowired
    private serv;

    @PostMapping(
        consumes = "application/json",
        produces = "application/json"
    )

        @RequestBody 

    @GetMapping(
        produces = "application/json"
    )